

export const ingresoValor = paylod =>({
    type:'INGRESO_VALOR',
    paylod,
})

export const estadoInicial = paylod =>({
    type:'ESTADO_INICIAL',
    paylod,
})

export const deshacerRehacer = paylod =>({
    type:'DESHACER_REHACER',
    paylod,
})